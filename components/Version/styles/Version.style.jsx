import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 10,
        right: 20
    },
    textVersion: {
        fontSize: 10,
        letterSpacing: -0.02
    }
})